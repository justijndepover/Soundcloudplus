<Page
    x:Class="SoundCloudPlus.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoundCloudPlus"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:SoundCloudPlus.ViewModels"
    xmlns:converters="using:SoundCloudPlus.Converters"
    mc:Ignorable="d">
    <Page.Resources>
        <viewModels:MainPageViewModel x:Key="MainPageViewModel" />
        <converters:IntToTimeConverter x:Key="IntToTimeConverter"/>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          DataContext="{StaticResource MainPageViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Background="#FFFF5500">
            <Button x:Name="NavButton" Style="{StaticResource btnNav}" Content="&#xE700;" Click="NavButton_Click" />
            <TextBlock Text="SoundCloud Plus" Style="{StaticResource AppTitle}" />
        </Grid>
        <SplitView x:Name="SplitView" Grid.Row="1">
            <SplitView.Pane>
                <ScrollViewer>
                    <StackPanel>
                        <Button Style="{StaticResource buttonsInMenu}" x:Name="AccountButton" Click="AccountButton_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Source="/Assets/Square44x44Logo.targetsize-24_altform-unplated.png"/>
                                <TextBlock Style="{StaticResource textInMenu}" Grid.Column="1" Text="Connect with SoundCloud" />
                            </Grid>
                        </Button>
                        <Button Style="{StaticResource buttonsInMenu}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Source="/Assets/Square44x44Logo.targetsize-24_altform-unplated.png"/>
                                <TextBlock Style="{StaticResource textInMenu}" Grid.Column="1" Text="Recent" />
                            </Grid>
                        </Button>
                        <Button Style="{StaticResource buttonsInMenu}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Source="/Assets/Square44x44Logo.targetsize-24_altform-unplated.png"/>
                                <TextBlock Style="{StaticResource textInMenu}" Grid.Column="1" Text="Artists" />
                            </Grid>
                        </Button>
                        <Button Style="{StaticResource buttonsInMenu}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Source="/Assets/Square44x44Logo.targetsize-24_altform-unplated.png"/>
                                <TextBlock Style="{StaticResource textInMenu}" Grid.Column="1" Text="Genres" />
                            </Grid>
                        </Button>
                        <Button Style="{StaticResource buttonsInMenu}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Source="/Assets/Square44x44Logo.targetsize-24_altform-unplated.png"/>
                                <TextBlock Style="{StaticResource textInMenu}" Grid.Column="1" Text="Following" />
                            </Grid>
                        </Button>
                        <Button Style="{StaticResource buttonsInMenu}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Source="/Assets/Square44x44Logo.targetsize-24_altform-unplated.png"/>
                                <TextBlock Style="{StaticResource textInMenu}"  Grid.Column="1" Text="Followers" />
                            </Grid>
                        </Button>
                        <Button Style="{StaticResource buttonsInMenu}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Source="/Assets/Square44x44Logo.targetsize-24_altform-unplated.png"/>
                                <TextBlock Style="{StaticResource textInMenu}" Grid.Column="1" Text="Playlists" />
                            </Grid>
                        </Button>
                        <Button Style="{StaticResource buttonsInMenu}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Source="/Assets/Square44x44Logo.targetsize-24_altform-unplated.png"/>
                                <TextBlock Style="{StaticResource textInMenu}" Grid.Column="1" Text="Likes" />
                            </Grid>
                        </Button>
                        <Button Style="{StaticResource buttonsInMenu}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Source="/Assets/Square44x44Logo.targetsize-24_altform-unplated.png"/>
                                <TextBlock Style="{StaticResource textInMenu}" Grid.Column="1" Text="My profile" />
                            </Grid>
                        </Button>
                        <Button Style="{StaticResource buttonsInMenu}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Source="/Assets/Square44x44Logo.targetsize-24_altform-unplated.png"/>
                                <TextBlock Style="{StaticResource textInMenu}" Grid.Column="1" Text="Settings" />
                            </Grid>
                        </Button>
                        <Button Style="{StaticResource buttonsInMenu}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Source="/Assets/Square44x44Logo.targetsize-24_altform-unplated.png"/>
                                <TextBlock Style="{StaticResource textInMenu}" Grid.Column="1" Text="About" />
                            </Grid>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
            </SplitView.Pane>
            <SplitView.Content>
                <ScrollViewer>
                    <RelativePanel>
                        <TextBlock x:Name="StreamTitle" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" Text="Stream" Style="{StaticResource SubTitle}"></TextBlock>
                        <GridView x:Name="StreamGridView" RelativePanel.Below="StreamTitle" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" ItemsSource="{Binding StreamCollection}" SizeChanged="StreamGridView_SizeChanged">
                            <GridView.ItemContainerStyle>
                                <Style TargetType="GridViewItem">
                                    <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                </Style>
                            </GridView.ItemContainerStyle>
                            <GridView.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource borderStyle1}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid Grid.Column="0" Grid.RowSpan="3" Grid.Row="0" Style="{StaticResource gridMusicStyleTop}">
                                                <Image x:Name="StreamItemUserImage" Source="{Binding User.AvatarUrl}" Style="{StaticResource imageAlbum}"/>
                                                <Image x:Name="StreamItemArtworkImage" Source="{Binding ArtworkUrl}" Style="{StaticResource imageAlbum}"/>
                                                <Grid Style="{StaticResource gridMusicStyleBottom}">
                                                    <TextBlock Text="{Binding Duration, Converter={StaticResource IntToTimeConverter}}" Style="{StaticResource AudioDuration}"/>
                                                </Grid>
                                            </Grid>
                                            <TextBlock x:Name="StreamItemTitle" Text="{Binding Title}" Grid.Column="1" TextWrapping="Wrap" Grid.Row="0"/>
                                            <TextBlock x:Name="StreamItemArtist" Grid.Column="1" TextWrapping="Wrap" Grid.Row="1">
                                            <Run Text="{Binding User.FullName}"/>
                                            <Run Text="{Binding User.Username}" Foreground="Gray"/>
                                            </TextBlock>
                                            <TextBlock Grid.Column="1" Grid.Row="2">
                                            <Run Text="&#xE768;" FontFamily="Segoe MDL2 Assets"/>
                                            <Run Text="{Binding PlaybackCount}" />
                                            <Run Text="  &#xE006;" FontFamily="Segoe MDL2 Assets"/>
                                            <Run Text="{Binding FavoritingsCount}"/>
                                            <Run Text="  &#xE134;" FontFamily="Segoe MDL2 Assets"/>
                                            <Run Text="{Binding CommentCount}"/>
                                            </TextBlock>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>
                        <TextBlock x:Name="ExploreTitle" RelativePanel.Below="StreamGridView" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" Text="Explore" Style="{StaticResource SubTitle}"></TextBlock>
                        <GridView x:Name="ExploreGridView" RelativePanel.Below="ExploreTitle" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" ItemsSource="{Binding ExploreCollection}" SizeChanged="ExploreGridView_SizeChanged">
                            <GridView.ItemContainerStyle>
                                <Style TargetType="GridViewItem">
                                    <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                </Style>
                            </GridView.ItemContainerStyle>
                            <GridView.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource borderStyle1}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid Grid.Column="0" Grid.RowSpan="3" Grid.Row="0" Style="{StaticResource gridMusicStyleTop}">
                                                <Image x:Name="ExploreItemUserImage" Source="{Binding User.AvatarUrl}" Style="{StaticResource imageAlbum}"/>
                                                <Image x:Name="ExploreItemArtworkImage" Source="{Binding ArtworkUrl}" Style="{StaticResource imageAlbum}"/>
                                                <Grid Style="{StaticResource gridMusicStyleBottom}">
                                                    <TextBlock Text="{Binding Duration, Converter={StaticResource IntToTimeConverter}}" Style="{StaticResource AudioDuration}"/>
                                                </Grid>
                                            </Grid>
                                            <TextBlock x:Name="ExploreItemTitle" Text="{Binding Title}" Grid.Column="1" TextWrapping="Wrap" Grid.Row="0"/>
                                            <TextBlock x:Name="ExploreItemArtist" Grid.Column="1" TextWrapping="Wrap" Grid.Row="1">
                                            <Run Text="{Binding User.FullName}"/>
                                            <Run Text="{Binding User.Username}" Foreground="Gray"/>
                                            </TextBlock>
                                            <TextBlock Grid.Column="1" Grid.Row="2">
                                            <Run Text="&#xE768;" FontFamily="Segoe MDL2 Assets"/>
                                            <Run Text="{Binding PlaybackCount}" />
                                            <Run Text="  &#xE006;" FontFamily="Segoe MDL2 Assets"/>
                                            <Run Text="{Binding FavoritingsCount}"/>
                                            <Run Text="  &#xE134;" FontFamily="Segoe MDL2 Assets"/>
                                            <Run Text="{Binding CommentCount}"/>
                                            </TextBlock>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>
                    </RelativePanel>
                </ScrollViewer>
                <!--<RelativePanel>
                    <GridView x:Name="StreamGridView" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" ItemsSource="{Binding StreamCollection}" SizeChanged="StreamGridView_SizeChanged">
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </GridView.ItemContainerStyle>
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource borderStyle1}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Column="0" Grid.RowSpan="3" Grid.Row="0" Style="{StaticResource gridMusicStyleTop}">
                                            <Image x:Name="StreamItemUserImage" Source="{Binding User.AvatarUrl}" Style="{StaticResource imageAlbum}"/>
                                            <Image x:Name="StreamItemArtworkImage" Source="{Binding ArtworkUrl}" Style="{StaticResource imageAlbum}"/>
                                            <Grid Style="{StaticResource gridMusicStyleBottom}">
                                            <TextBlock Text="{Binding Duration, Converter={StaticResource IntToTimeConverter}}" Style="{StaticResource AudioDuration}"/>
                                        </Grid>
                                        </Grid>
                                        <TextBlock x:Name="StreamItemTitle" Text="{Binding Title}" Grid.Column="1" TextWrapping="Wrap" Grid.Row="0"/>
                                        <TextBlock x:Name="StreamItemArtist" Grid.Column="1" TextWrapping="Wrap" Grid.Row="1">
                                            <Run Text="{Binding User.FullName}"/>
                                            <Run Text="{Binding User.Username}" Foreground="Gray"/>
                                        </TextBlock>
                                        <TextBlock Grid.Column="1" Grid.Row="2">
                                            <Run Text="&#xE768;" FontFamily="Segoe MDL2 Assets"/>
                                            <Run Text="{Binding PlaybackCount}" />
                                            <Run Text="  &#xE006;" FontFamily="Segoe MDL2 Assets"/>
                                            <Run Text="{Binding FavoritingsCount}"/>
                                            <Run Text="  &#xE134;" FontFamily="Segoe MDL2 Assets"/>
                                            <Run Text="{Binding CommentCount}"/>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>
                    <GridView x:Name="ExploreGridView" RelativePanel.Below="StreamGridView" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" ItemsSource="{Binding ExploreCollection}" SizeChanged="ExploreGridView_SizeChanged">
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </GridView.ItemContainerStyle>
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource borderStyle1}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Column="0" Grid.RowSpan="3" Grid.Row="0" Style="{StaticResource gridMusicStyleTop}">
                                            <Image x:Name="ExploreItemUserImage" Source="{Binding User.AvatarUrl}" Style="{StaticResource imageAlbum}"/>
                                            <Image x:Name="ExploreItemArtworkImage" Source="{Binding ArtworkUrl}" Style="{StaticResource imageAlbum}"/>
                                            <Grid Style="{StaticResource gridMusicStyleBottom}">
                                                <TextBlock Text="{Binding Duration, Converter={StaticResource IntToTimeConverter}}" Style="{StaticResource AudioDuration}"/>
                                            </Grid>
                                        </Grid>
                                        <TextBlock x:Name="ExploreItemTitle" Text="{Binding Title}" Grid.Column="1" TextWrapping="Wrap" Grid.Row="0"/>
                                        <TextBlock x:Name="ExploreItemArtist" Grid.Column="1" TextWrapping="Wrap" Grid.Row="1">
                                            <Run Text="{Binding User.FullName}"/>
                                            <Run Text="{Binding User.Username}" Foreground="Gray"/>
                                        </TextBlock>
                                        <TextBlock Grid.Column="1" Grid.Row="2">
                                            <Run Text="&#xE768;" FontFamily="Segoe MDL2 Assets"/>
                                            <Run Text="{Binding PlaybackCount}" />
                                            <Run Text="  &#xE006;" FontFamily="Segoe MDL2 Assets"/>
                                            <Run Text="{Binding FavoritingsCount}"/>
                                            <Run Text="  &#xE134;" FontFamily="Segoe MDL2 Assets"/>
                                            <Run Text="{Binding CommentCount}"/>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>
                </RelativePanel>-->
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>