<Page
    x:Class="SoundCloudPlus.Pages.FollowingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoundCloudPlus.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:SoundCloudPlus.ViewModels"
    mc:Ignorable="d">
    <Page.Resources>
        <viewModels:FollowingPageViewModel x:Key="FollowingViewModel" />
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" DataContext="{StaticResource FollowingViewModel}">
        <ScrollViewer VerticalScrollBarVisibility="Hidden">
            <RelativePanel>
                <TextBlock x:Name="FollowingTitle" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" Text="Following" Style="{StaticResource SubTitle}"/>
                <GridView x:Name="FollowingGridView" RelativePanel.Below="FollowingTitle" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" ItemsSource="{Binding FollowingsCollection}" ScrollViewer.VerticalScrollMode="Disabled" ScrollViewer.HorizontalScrollMode="Disabled">
                    <GridView.ItemContainerStyle>
                        <Style TargetType="GridViewItem">
                            <Setter Property="VerticalContentAlignment" Value="Stretch" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </GridView.ItemContainerStyle>
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="AvatarUrl: "></TextBlock>
                                <Image Source="{Binding AvatarUrl}" Style="{StaticResource ImageAlbum}"/>
                                <TextBlock Text="Username: "></TextBlock>
                                <TextBlock Text="{Binding Username}"></TextBlock>
                                <TextBlock Text="Following: "></TextBlock>
                                <TextBlock Text="{Binding FollowingsCount}"></TextBlock>
                                <TextBlock Text="Link: "></TextBlock>
                                <TextBlock Text="{Binding PermalinkUrl}"></TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </RelativePanel>
        </ScrollViewer>
    </Grid>
</Page>
